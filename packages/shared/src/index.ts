/**
 * Types shared by the backend and the extension.
 *
 * Intentionally empty at M0. The API request/response contracts land here in
 * M4 (search) and M3/M5 (video job status), so both sides stay in sync.
 */

export {};
